# Registro 2026-05-20

## Estado para retomar

Repositorio limpio al cierre, dejando fuera solo `.tmp/` como carpeta temporal local.

Ultimo commit esperado en produccion:

- `c682f7d` - `Send large attachments through Blob links`

URL de trabajo:

- https://febo-ai.vercel.app

## Sesion 20/05 - UI mobile y desktop estilo Hariaz

Se ajusto:

- Mobile:
  - Logo compacto con `F`.
  - Buscador mobile con lupa y texto `Buscar`.
  - Detalle de conversacion con `Volver` compacto.
  - Cabecera mobile con `IA Activa` y `Transferir`.
  - Nombre del contacto visible sin editor.
  - Boton lateral `Conversaciones` vuelve al listado.
  - Se oculto `Enviar evento` en mobile.
  - Acciones de etiqueta/resumen/plantilla/respuestas rapidas alineadas junto al nombre.
  - Modal de transferencia mas ancho y estable.
  - Pestañas `Conversacion` / `Notas internas` ajustadas para ocupar ancho completo.

- Desktop:
  - `Conversacion` y `Notas internas` se movieron a la base del chat.
  - Composer inferior con botones de pestaña, microfono, adjuntar, campo de respuesta y enviar.
  - Drop de archivos en desktop se expandio al panel grande de conversacion.
  - Se quito el recuadro fijo de `Solta aca...` en desktop.

## Sesion 20/05 - Tarjetas, etiquetas y asignacion

Se ajusto:

- Etiquetas de tarjetas con colores mas suaves y translucidos.
- Se elimino el pill `asignado: Sin asignar`.
- Si hay operador real, se muestra `asignado: Nombre`.
- Si no hay operador asignado, no se muestra nada.

## Sesion 20/05 - Archivos, imagenes y videos

Se corrigio:

- Imagenes verticales salientes:
  - Se normaliza orientacion EXIF con `sharp` antes de subir a WhatsApp.
  - Las fotos verticales se mandan ya rotadas correctamente.

- Videos MP4:
  - Se acepta `.mp4` por extension aunque el navegador mande MIME raro o vacio.
  - Se normaliza el tipo como `video/mp4`.
  - Si WhatsApp rechaza enviar como video, se intenta enviar como documento MP4.
  - Para archivos grandes se agrego subida directa a Vercel Blob y envio por link.

Pendiente operativo:

- Activar Vercel Blob en el proyecto si no esta activo.
- Configurar `BLOB_READ_WRITE_TOKEN` en Vercel para habilitar subida directa de videos grandes.
- Luego probar de nuevo el MP4 que marco limite de subida directa.

## Verificacion

Durante la sesion se corrio varias veces:

- `npm.cmd run typecheck` OK.
- `npm.cmd run build` OK.
- Deploys en Vercel confirmados `READY`.
- Produccion respondio `200 OK`.

